package com.capgemini.reflection.basic_level.dynamically_create_objects;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class StudentTest {
    @Test
    public void testDynamicallyCreateObject() throws Exception{
//        // Creating the student class object without instance of class
//        Class<?> studentClass = Class.forName("com.capgemini.reflection.basic_level.dynamically_create_objects.Student");
//        Student student = (Student) studentClass.getConstructor(String.class).newInstance("Bipin",2);
//        // Display the result
//
//        assertEquals("Bipin",student.getName());
    }

}